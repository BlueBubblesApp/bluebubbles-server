export type Config = {
    socket_port: number;
    server_address: string;
    ngrok_key: string;
    password: string;
    tutorial_is_done: boolean;
    auto_start: boolean;
    auto_caffeinate: boolean;
};
