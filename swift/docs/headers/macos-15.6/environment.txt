# NOT collected by Tools/private-api/collect.sh.
#
# Every other directory here is a runtime dump, and its environment.txt describes the Mac it
# was taken on. This one is a borrowed third-party class-dump of the shipping binaries,
# transcribed from developer.limneos.net, and this file exists to say so in the same place a
# reader looks for provenance. See README.md, "macOS 15.6 is a borrowed dump".
#
# The fields below describe the machine LIMNEOS dumped, not one of ours, and the ones a
# runtime dump would measure on the spot are unknown rather than defaulted.

macos_version   15.6
macos_build     24G84
architecture    arm64e
hardware        Macmini9,1
xcode           n/a — not built here
clang           n/a — not built here
sdk             n/a — read from the dyld shared cache, not compiled against an SDK
source          https://developer.limneos.net/index.php?ios=macos_15.6
tool            classdump-dyld 3.0
transcribed     2026-03-25

# Host apps. In a collected dump this is the most useful block in the file, because the
# app's Mach-O decides WHICH COPY of a shared framework the dumper sees. Here nothing was
# executed and no app was read, so there is nothing to report — and that absence is the
# point: these headers are the NATIVE macOS frameworks, whereas Messages.app is a Catalyst
# app and loads the /System/iOSSupport copies. Where the two differ, this directory
# describes the copy Messages does NOT run.
# bundle-id            installed  platform   version
app com.apple.MobileSMS    unknown    -          -
app com.apple.FaceTime     unknown    -          -
app com.apple.findmy       unknown    -          -
app com.apple.Notes        unknown    -          -

# Classes recorded as NOT PRESENT. Read these with more care than a collected dump's list:
# limneos indexes the native shared cache, so a class that lives only under /System/iOSSupport
# is absent from the SOURCE, not necessarily from macOS 15.6. ChatKit is the whole of that
# problem — it has no native copy at all, so none of its classes could be transcribed and the
# entire send path is simply undumped here. README.md names the 26 classes this affects.
missing FindMyLocate.FenceServiceDaemonXPC
missing FindMyLocate.FriendshipServiceClientXPC
missing FindMyLocate.LocationServiceClientXPC
missing IMChatInfo
